<h1>Add a new film to the list </h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
<%= f.association :movie %>
<%= f.input :comment %>
  <%= f.submit "Submit a bookmark", class: "btn btn-primary" %>
<% end %>
